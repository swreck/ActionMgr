[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npx prisma generate && npx tsc && cd ../frontend && npm install && npm run build && cp -r dist ../backend/public"

[deploy]
startCommand = "cd backend && npx prisma db push --skip-generate && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
